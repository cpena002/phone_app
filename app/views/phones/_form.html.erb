<%= form_for(@phone) do |f| %>
  <% if @phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
      <% @phone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :home_number %><br>
    <%= f.number_field :home_number %>
  </div>
  <div class="field">
    <%= f.label :cell_number %><br>
    <%= f.number_field :cell_number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
